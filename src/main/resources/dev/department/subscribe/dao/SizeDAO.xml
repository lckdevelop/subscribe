<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="dev.department.subscribe.dao.SizeDAO">
	<select id="getClothSize" resultType="SizeDTO">
		select no, productsize as productSize
		from clothsize	
	</select>
	
	<select id="getShoeSize" resultType="SizeDTO">
		select no, productsize as productSize
		from shoesize
	</select>


</mapper>